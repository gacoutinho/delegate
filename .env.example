# Copy this to .env.local and fill it in.
# Anthropic API key — used by the Claude Agent SDK to run the agents.
# Get one at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# (Optional) Default model used when an agent doesn't specify one.
DELEGATE_DEFAULT_MODEL=claude-opus-4-8
