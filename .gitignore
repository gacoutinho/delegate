# deps
node_modules
.pnp
.pnp.js

# next
.next
out
build
*.tsbuildinfo

# env (NEVER commit the API key)
.env
.env.local
.env*.local

# connector credentials (tokens) — local only
connectors.local.json

# files generated by agents at runtime
/workspace/*
!/workspace/.gitkeep

# os
.DS_Store
*.log
